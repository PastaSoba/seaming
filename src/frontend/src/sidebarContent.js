import React, {Component} from 'react';


export class SidebarContent extends Component {
    constructor(props){
        super(props);
    }

    render(){
        if (this.props.name !== ""){
            return (null
            )
        }
        return (null)
    }
}